import {useEffect,useState} from 'react';import {FlatList,SafeAreaView,Text,View} from 'react-native';
const API=process.env.EXPO_PUBLIC_API_URL||'http://localhost:3000';
export default function Candidates(){const [data,setData]=useState<any[]>([]);const [error,setError]=useState('');useEffect(()=>{fetch(`${API}/api/candidaturas`).then(r=>r.json()).then(x=>setData(x.data||[])).catch(()=>setError('Não foi possível consultar a API.'))},[]);return <SafeAreaView style={{flex:1,padding:20}}><Text style={{fontSize:28,fontWeight:'800',marginBottom:16}}>Candidaturas</Text>{error?<Text>{error}</Text>:<FlatList data={data} keyExtractor={x=>x.id} renderItem={({item})=><View style={{paddingVertical:14,borderBottomWidth:1}}><Text style={{fontSize:18,fontWeight:'700'}}>{item.ballot_name}</Text><Text>{item.office} · {item.party} · {item.registration_status}</Text><Text>Fonte: TSE · {item.source_updated_at?new Date(item.source_updated_at).toLocaleString('pt-BR'):''}</Text></View>}/>}</SafeAreaView>}
