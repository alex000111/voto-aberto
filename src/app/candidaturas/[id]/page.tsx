import Link from 'next/link';import {notFound} from 'next/navigation';import {getCandidate,getCandidateChanges,getCandidateHistory} from '@/lib/candidates';import {candidateOfficialResources} from '@/lib/candidate-resources';
export const dynamic='force-dynamic';
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const c=await getCandidate(id);if(!c)notFound();const [history,changes]=await Promise.all([getCandidateHistory(id),getCandidateChanges(id)]);let official:any=null;try{official=await candidateOfficialResources(c.uf||c.scope)}catch{};return <main className="page"><Link className="back" href="/candidaturas">← Candidaturas</Link><div className="profileHead"><div><div className="eyebrow">{c.office} • {c.uf||c.scope}</div><h1>{c.ballot_name}</h1><p className="lead">{c.full_name}</p></div><div className="ballot"><strong>{c.number||'—'}</strong><span>{c.party||'Partido não informado'}</span></div></div><div className="profileGrid"><section className="card"><span className="tag">SITUAÇÃO REGISTRADA</span><h3>{c.registration_status||'Não informada'}</h3><p>Última sincronização: {c.source_updated_at?new Date(c.source_updated_at).toLocaleString('pt-BR',{timeZone:'America/Recife'}):'não disponível'}.</p></section><section className="card"><span className="tag">PROVENIÊNCIA</span><h3>Tribunal Superior Eleitoral</h3><p>O Voto Aberto preserva versões e não converte situação de registro em avaliação sobre a candidatura.</p></section></div><section className="section"><h2>Documentos oficiais disponíveis</h2><p>Recursos publicados pelo TSE para a circunscrição desta candidatura. A presença do recurso não implica análise ou avaliação pelo Voto Aberto.</p>{official?<div className="profileGrid"><section className="card"><span className="tag">PROPOSTA DE GOVERNO</span><h3>{official.proposals.length?'Disponível na fonte oficial':'Não localizada para esta circunscrição'}</h3>{official.proposals.slice(0,2).map((r:any)=><a key={r.id} href={r.url} target="_blank" rel="noreferrer">Abrir recurso do TSE</a>)}</section><section className="card"><span className="tag">OUTROS RECURSOS</span><h3>Fotos, certidões e dados complementares</h3><p>{official.photos.length} recurso(s) de foto • {official.certificates.length} recurso(s) de certidões.</p></section></div>:<div className="placeholder">Catálogo oficial temporariamente indisponível. Nenhuma ausência é inferida a partir desta falha.</div>}</section><section className="section"><h2>Histórico auditável</h2><p>Alterações detectadas no registro ingerido. O histórico permite verificar quando um campo mudou na nossa base.</p>{changes.length?<div className="timeline">{changes.map((x:any)=><div key={x.id}><b>{new Date(x.detected_at).toLocaleString('pt-BR',{timeZone:'America/Recife'})}</b><span>{x.details?.status||'Registro atualizado'} • {x.details?.party||c.party}</span></div>)}</div>:<div className="placeholder">Ainda não há mudanças posteriores registradas para este perfil.</div>}<p className="stamp">Versões preservadas: {history.length}</p></section></main>}
